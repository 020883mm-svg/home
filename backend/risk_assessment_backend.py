import sqlite3\n\n# Database class for home insurance risk assessment\nclass Database:\n    def __init__(self, db_name='risk_assessment.db'):\n        self.conn = sqlite3.connect(db_name)\n        self.create_tables()\n\n    def create_tables(self):\n        cursor = self.conn.cursor()\n        cursor.execute('''CREATE TABLE IF NOT EXISTS properties (id INTEGER PRIMARY KEY, address TEXT, construction_year INTEGER, material TEXT)''')\n        cursor.execute('''CREATE TABLE IF NOT EXISTS assessments (id INTEGER PRIMARY KEY, property_id INTEGER, risk_score REAL, FOREIGN KEY (property_id) REFERENCES properties (id))''')\n        cursor.execute('''CREATE TABLE IF NOT EXISTS photos (id INTEGER PRIMARY KEY, property_id INTEGER, photo_url TEXT, FOREIGN KEY (property_id) REFERENCES properties (id))''')\n        cursor.execute('''CREATE TABLE IF NOT EXISTS renovation_history (id INTEGER PRIMARY KEY, property_id INTEGER, renovation_date TEXT, details TEXT, FOREIGN KEY (property_id) REFERENCES properties (id))''')\n        self.conn.commit()\n\n# Building Registry Scraper class for Estonian Building Registry integration\nclass BuildingRegistryScraper:\n    def scrape_building_data(self, address):\n        # Integration with building registry API would go here\n        # For demonstration returning dummy data\n        return {'construction_year': 1980, 'material': 'brick'}\n\n# Cloud Photo Storage class for AWS S3 integration\nclass CloudPhotoStorage:\n    def __init__(self, bucket_name):\n        self.bucket_name = bucket_name\n        # Initialize AWS S3 client here\n\n    def upload_photo(self, file_path):\n        # Upload logic to S3 would go here\n        # For demonstration returning dummy URL\n        return f'https://{self.bucket_name}.s3.amazonaws.com/{file_path}'\n\n# Risk Assessment Engine class\nclass RiskAssessmentEngine:\n    def __init__(self):\n        self.risk_thresholds = {'HIGH': (70, 100), 'NORMAL': (40, 69), 'LOW': (0, 39)}\n\n    def calculate_risk_score(self, property_data):\n        score = 0\n        score += (2026 - property_data['construction_year']) * 0.2\n        score += self.weight_material(property_data['material']) * 0.15\n        # Other scoring metrics would be included here\n        return score\n\n    def weight_material(self, material):\n        if material == 'brick':\n            return 10\n        return 5\n\n    def determine_risk_category(self, score):\n        for category, (low, high) in self.risk_thresholds.items():\n            if low <= score <= high:\n                return category\n        return 'UNKNOWN'\n\n# Home Insurance Underwriter orchestrator class\nclass HomeInsuranceUnderwriter:\n    def __init__(self):\n        self.database = Database()\n        self.scraper = BuildingRegistryScraper()\n        self.photo_storage = CloudPhotoStorage('my-bucket')\n        self.risk_assessment_engine = RiskAssessmentEngine()\n\n    def assess_property(self, address, file_path):\n        building_data = self.scraper.scrape_building_data(address)\n        photo_url = self.photo_storage.upload_photo(file_path)\n        risk_score = self.risk_assessment_engine.calculate_risk_score(building_data)\n        risk_category = self.risk_assessment_engine.determine_risk_category(risk_score)\n\n        # Save results to database\n        return {'risk_score': risk_score, 'risk_category': risk_category, 'photo_url': photo_url}\n\n# Example usage\nif __name__ == '__main__':\n    underwriter = HomeInsuranceUnderwriter()\n    result = underwriter.assess_property('Tallinn, Estonia', 'home_photo.jpg')\n    print(result)\n